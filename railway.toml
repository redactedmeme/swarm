[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3

[env]
PYTHONUNBUFFERED = "1"
LOG_LEVEL = "INFO"
SWARM_MODE = "delegation"
