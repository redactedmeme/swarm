[build]
builder = "dockerfile"

[deploy]
startCommand = "python python/hermes_delegation_executor.py --manifest fs/clawtasks_v1.json --mode dispatch --output fs/clawtask_dispatch_results.json"

[env]
PYTHONUNBUFFERED = "1"
LOG_LEVEL = "INFO"
SWARM_MODE = "delegation"
