#!/usr/bin/env python3
"""Minimal test app to verify deployment works."""
import sys
print("TEST APP STARTED", file=sys.stderr)
print("Python:", sys.version, file=sys.stderr)
sys.exit(0)  # Exit immediately to see if this runs
