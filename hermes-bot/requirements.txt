# hermes-bot/requirements.txt
# REDACTED Swarm Manager — powered by Hermes Agent framework
hermes-agent[messaging]>=0.13.0
redis>=5.0.0

# Legacy deps (kept for fallback mode via HERMES_MODE=legacy)
python-telegram-bot[webhooks]==20.7
aiohttp==3.9.1
requests==2.31.0
python-dotenv==1.0.0
APScheduler>=3.10
groq>=0.4.0
PyYAML>=6.0
tweepy>=4.14
