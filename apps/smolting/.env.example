
# ── Holder-gated alpha feed ───────────────────────────────────────────────────
# Where the daily report is pushed, and the secret to present. When BOTH are set
# the public group gets only a teaser linking to ALPHA_PAGE_URL; when either is
# missing the full report is posted to the group as before.
ALPHA_PUBLISH_URL=https://terminal.redacted.meme/api/alpha/publish
ALPHA_PUBLISH_TOKEN=
ALPHA_PAGE_URL=https://terminal.redacted.meme/
