# mcp — unstarted stub

A `package.json` declaring `@phantom/mcp-server` and nothing else: no `index.js`,
no deployment, no importers. Kept because it records an intent, not because it
runs. See `docs/history/CONSOLIDATION_SUMMARY.md`, which reached the same
conclusion when `services/` was last audited.

Delete it if the Phantom MCP server is not going to be built.
