python-telegram-bot[job-queue]==20.7
python-dotenv==1.0.0
# >=3.10: the current `openai` vendors httpx_aiohttp, which needs
# aiohttp.SocketTimeoutError. Pinning 3.9.1 downgrades what the shared
# packages install and breaks their import — see fd36242.
aiohttp>=3.10
requests>=2.31.0
redis>=5.0.0
