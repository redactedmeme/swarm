# Not pinned to 3.9.x: the openai client the shared packages install vendors
# httpx_aiohttp, which needs aiohttp>=3.10 (AttributeError: SocketTimeoutError).
aiohttp>=3.10
python-dotenv==1.0.0
redis>=5.0.0
